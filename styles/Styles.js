import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    maxWidth: 448,
    width: "100%",
    backgroundColor: "#59ADFF",
    margin: "auto",
  },

  title: {
    fontSize: 10,
    color: "#000",
    backgroundColor: "#fff",
  },

  button: {
    fontSize: 12,
    color: "#000",
    backgroundColor: "#fff",
  },
});
